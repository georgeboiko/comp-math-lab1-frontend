import ApproxForm from "../shared/ApproxForm"

function Logarithmic() {
    return <ApproxForm title="ЛОГАРИФМИЧЕСКАЯ АППРОКСИМАЦИЯ  φ(x) = a·ln(x) + b" url="/api/lab/4/logarithmic" isAll={false} />
}

export default Logarithmic
