import ApproxForm from "../shared/ApproxForm"

function Cubic() {
    return <ApproxForm title="КУБИЧЕСКАЯ АППРОКСИМАЦИЯ  φ(x) = a₀ + a₁x + a₂x² + a₃x³" url="/api/lab/4/cubic" isAll={false} />
}

export default Cubic
