import ApproxForm from "../shared/ApproxForm"

function Linear() {
    return <ApproxForm title="ЛИНЕЙНАЯ АППРОКСИМАЦИЯ  φ(x) = ax + b" url="/api/lab/4/linear" isAll={false} />
}

export default Linear
