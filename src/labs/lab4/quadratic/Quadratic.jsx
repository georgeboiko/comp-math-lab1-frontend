import ApproxForm from "../shared/ApproxForm"

function Quadratic() {
    return <ApproxForm title="КВАДРАТИЧНАЯ АППРОКСИМАЦИЯ  φ(x) = a₀ + a₁x + a₂x²" url="/api/lab/4/quadratic" isAll={false} />
}

export default Quadratic
